# Workbench: CooksAssistant / feature/import-to-bdtv-factory

> Auto-generated by BDTV Factory. Do not edit.

## App URL
http://localhost:3140

## Allocated Ports
- **app**: http://localhost:3140 (container port 3000)
- **db**: http://localhost:3141 (container port 5432)

## Running the app
- **F5** in VSCode — starts docker services then runs the app locally
- **Ctrl+Shift+B** — starts docker services only (`docker compose up`)

## Planning a feature
Use Plan mode (Shift+Tab+Tab) to design changes before implementing.
Always generate a Mermaid diagram alongside the plan via `/mermaid-ledger`
— it visualises what will be created, modified, or deleted before any
code is written.

Diagrams are saved to `ledger/` in this workshop and can be viewed with:

```
bdtv-factory ledger
```

## Debugging
Run `bdtv-factory run debug` to start an autonomous Playwright investigation.
The @playwright-debugger agent reads the Debug Context in this workshop's CLAUDE.md.
